pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = "MinecraftFpsEngine"
include(":app")   // Remove :mod if present
